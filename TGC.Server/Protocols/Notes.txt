Authentication:
To get an auth token for a client, take the GUID generated by the server, and append your key sent by the server on connecting to the websocket port. Then, compute the MD5 hash of that, and base64 encode it. That base64 string is the token.